# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Dependencies & build output
node_modules
dist
dist-ssr
*.local

# Environment / secrets
.env
.env.local
.env.*.local

# Claude Code — keep shared config, ignore personal overrides
.claude/settings.local.json

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.sw?

# Feedback digests — kids' free text stays LOCAL, never in the public repo
docs/feedback-digests/
